//---------------------------------------------------------------------------

#pragma hdrstop

#include "Random.h"
//---------------------------------------------------------------------------
#pragma package(smart_init)


Random::Random(){
	for (int i = 0; i < 26; ++i) {
		TableauStat[i] = 100/26;
	}
}

void Random::Erreur(int Index){
	for (int i = 0; i < 25; i++) {
		if (i == Index) TableauStat[i] += 5.2;
		else TableauStat[i] -= 0.2;
	}
}

void Random::Reussite(int Index){
	if(TableauStat[Index] < 1){
		float Valeur = ((TableauStat[Index]-1)/2)/25;
		for(int i = 0; i<25 ; i++){
			if(i == Index) TableauStat[Index] = (TableauStat[Index]-1)/2;
			else TableauStat[Index] += Valeur;
		}
	}
}

int Random::Tirage(float PlageProbaTire){

	float value = 0.0;
	for (int i = 0; i < 25; i++) {
		value = TableauStat[i]

	}

}
